server {
        listen 80;
        server_name vibes.ryanjpeterson.dev;

        location / {
            # Proxy to the frontend container running on localhost:3000
            proxy_pass http://localhost:3000;
            
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
    }
    ```

5.  **Enable the site:**
    ```bash
    sudo ln -s /etc/nginx/sites-available/vibes.ryanjpeterson.dev /etc/nginx/sites-enabled/
    ```

6.  **Test and Reload Nginx:**
    ```bash
    sudo nginx -t
    sudo systemctl reload nginx
    ```

7.  **Set up SSL with Certbot (Highly Recommended):**
    ```bash
    sudo certbot --nginx -d vibes.ryanjpeterson.dev